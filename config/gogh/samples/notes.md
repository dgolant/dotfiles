# Gogh samples

Sample files powering the `colors` theme preview.

- `app.js` — code, highlighted by bat
- `config.json` — data, colored by jq
- `app.log` — logs (view with `lnav`)

## Notes

> The quick brown fox jumps over the lazy dog.

Run `gogh fav -c` to favorite the active theme.
